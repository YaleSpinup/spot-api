{ 
  "listenAddress": ":8080",
  "accounts": {
    "{{ .spot_account_id }}": {
      "region": "us-east-1",
      "id": "{{ .spot_account_id }}",
      "token": "{{ .spot_token }}"
    }
  },
  "token": "{{ .api_token }}",
  "logLevel": "{{ .log_level }}",
  "org": "{{ .spinup_org }}"
}
